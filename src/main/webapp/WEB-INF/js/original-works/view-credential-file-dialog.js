function setViewCredentialFileImage(src) {
    var imageField = $('.view-credential-file-dialog .view-credential-file-image');
    imageField.prop('src', src);
}

$(document).ready(function() {
    var externalUrl = $('#external-url').val();
});